<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Tasks extends CI_Controller {

	//@TODO task handling...
	//function runtask($id){}
	
/*	function index(){
		$this->load->library('task');
		//$this->task->loadTask(1);
		//echo var_dump($this->session->userdata('task'));
		
		if($this->task->isLoaded()) $this->task->run_next();
		else {
			//figure out what task to load and load it
			$this->task->loadTask(1);
			$this->task->run_next();
		}
		
	}
	
	function start($tid, $subject)
	{
		//get demographic form
		//insert results set and store id in session
		//run(tid)
		
	}	
	
	function run($tid)
	{
		//$this->destroy();
		$this->load->library('task');
		$this->task->loadTask($tid);
		$this->task->run_next();
	}
	

	function destroy()
	{
		$this->session->sess_destroy();
		//echo "session destroyed";
	}


*/

}